[build]
  publish = "dist"
  ignore = "exit 0"  # Always skip Netlify's own build — GitHub Action handles it

[[headers]]
  for = "/*"
  [headers.values]
    Cache-Control = "public, max-age=0, must-revalidate"
    X-Frame-Options = "SAMEORIGIN"
    X-Content-Type-Options = "nosniff"
